{% extends "::layout.html.twig" %}

{% block title %}
	Ajouter une annonce - {{ parent()}}
{% endblock %}

{% block body %}
	<section class="view" id="add">
		<h1>Ajouter une annonce : {% if title == 'rent' %}Location{% else %}Vente{% endif %}</h1>

		{% if title == 'rent' %}
	  		{{ include("MangoPlatformBundle:User:formRent.html.twig") }}
	  	{% else %}
	  		{{ include("MangoPlatformBundle:User:formBuy.html.twig") }}
	  	{% endif %}
	  	
	    <a href="{{ path('mango_platform_user') }}" class="btn btn-default col-12 col-md-4">Retour à mes annonces</a>
  	</section>
{% endblock %}