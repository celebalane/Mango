{% extends "UserBundle::layout.html.twig" %}

{% block fos_user_content %}
	<section class="contenu" id="reset">
		<h1>Réinitialisation du mot de passe</h1>
		<div class="d-flex justify-content-center">
			{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'col-12 col-md-6 col-lg-6' } }) }}
			    <div class="form-group">
		      		{{ form_label(form.plainPassword.first, "Mot de passe") }}
		      		{{ form_errors(form.plainPassword.first) }}
		        	{{ form_widget(form.plainPassword.first, {'attr': {'class': 'form-control'}}) }}
		    	</div>
		    	<div class="form-group">
		      		{{ form_label(form.plainPassword.second, "Confirmation du mot de passe") }}
		      		{{ form_errors(form.plainPassword.second) }}
		        	{{ form_widget(form.plainPassword.second, {'attr': {'class': 'form-control'}}) }}
		    	</div>
		    	<input type="hidden" id="fos_user_resetting_form__token" name="fos_user_resetting_form[_token]" value="k4rpB5bkVV031N4XfKKmGaJxu4un8EOM1xbq4dImT4Q" />
		    	{{ form_rest(form) }}
			    <div>
			        <input type="submit" value="Envoyer" class="btn" />
			    </div>
			{{ form_end(form) }}
		</div>
	</section>
{% endblock %}